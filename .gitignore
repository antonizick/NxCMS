# Dependencies — bundled into release zips, not committed
/vendor/

# Secrets and per-install state
.env
/storage/app.env
/storage/installed.lock
/storage/install-token.txt

# User uploads
/storage/uploads/*
!/storage/uploads/.gitkeep

# Build output
/dist/
/build/

# Lucent-internal project-mode files — never published
/CLAUDE.md
/AGENTS.md
/.lucentrc
/planning.md

# Real origin-identity values for leakcheck.sh — never published
/tools/leakcheck.local.env

*.log
.DS_Store
Thumbs.db
